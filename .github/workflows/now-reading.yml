name: Now reading
on:
  repository_dispatch:
    types: [now-reading]
jobs:
  myEvent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          ${{ github.event.client_payload.github }}" > _data/now-reading.json
